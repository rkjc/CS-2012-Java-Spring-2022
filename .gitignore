 


 
bin/
.git
.metadata
.classpath
.project
*.backup
*.kate-swp
*.old
.directory


# a bash function for doing quick gitHub updates
#  cupdate() {  git add . ; git commit -m "quick code update" ; git push origin main ; } 

# note to self because I always forget this one when resetting the .gitignore
#  git rm -r --cached . 

# shows the files that have changed and need to be staged and commited
#  git ls-files --others --exclude-standard

# list by only name$  ls -1a

#*have to run this every time terminal is started (need to script this)*
# eval "$(ssh-agent -s)" 
# ssh-add ~/.ssh/id_ed25519_github  
# testing:    ssh -T git@github.com 
